1. 在启用虚拟地址下实现sys_get_time和sys_task_info 实现sys_mmap和sys_unmap系统调用
2. 

    1. SV39由ppn和标注位构成
      - V表示页表项是否有效
      - R/W/X表示页表项指向的页是否可读/可写/可执行
      - U表示页表项指向的页在用户模式下是否可访问
      - A(Accessed) 记录自从页表项上的这一位被清零之后，页表项的对应虚拟页面是否被访问过
      - D(Dirty) 则记录自从页表项上的这一位被清零之后，页表项的对应虚拟页表是否被修改过
    2. 
      - 访问一个无效的页,页表中不存在相应的映射
      - scause, 描述trap的原因
      - 当内存被访问时再申请内存, 防止应用申请过多的内存, 而只使用其中一小部分的内存, 节省内存
      - 20MB
      - 在内存分配时不立即分配实际物理内存，只记录映射请求。当缺页发生时, 检测scause的值, 和发生缺页的地址, 判断是否是缺页异常, 如果是缺页异常, 分配一个page, 并在page table中添加映射
      - 有效位置零
    3. 
      - 不需要切换页表
      - 内核页不设置U标志位
      - 内核和用户共享同一张页表，不需要维护两个独立的页表结构，节省了内存开销
      - 当用户程序trap到内核态时, 或内核态返回到用户态时
        进程切换时：在不同进程间切换时更换页表，以映射该进程的内存空间。
        内核初始化时：系统启动时，内核初始化页表，之后在进程切换时切换到相应的页表。

1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：
2. 此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：
3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。
4.  我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计

